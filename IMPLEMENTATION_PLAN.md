# KMS 연결 그래프 구현 계획

작성·최종 검수: Astra. 구현: Sol(그래프 엔진·화면), Terra(데이터 연결·회귀 테스트).

## 목표와 범위

합의한 실제 그래프 뷰와 이를 사용하는 자료 목록/상세 화면을 저장소에 구현한다. 작은 점·가는 선, 자동 배치 후 안정화, 부드러운 확대·이동, 겹치지 않는 점진적 이름 표시, 선택 주변 강조, 전체/주변 탐색이 완료 기준이다.

지식·문서(document), 업무 도구(tool), AI 자산(ai_asset)을 구분한다. 프롬프트는 AI 자산 중 프롬프트 자체를 등록한 항목에만 존재한다. 업무 분야·부서·태그는 메타데이터이며 자동 노드로 만들지 않는다. 관계는 명시적인 참조·근거·사용법 등으로 생성한다.

기존 CSV/Sheets/Notion 수집 흐름을 새 화면에 연결한다. 이 작업은 읽기 중심 KMS와 그래프의 구현이며, SSO·서버 저장/공동 편집·파일 업로드·AI 실행·운영 배포는 이번 그래프 작업의 범위가 아니다. 운영 배포나 외부 서비스 변경은 수행하지 않는다. 정적 산출물은 읽는 사람이 전체 포함 데이터에 접근하므로 문서별 권한을 제공한다고 표시하지 않는다.

## 구조

- `frontend/`: React + TypeScript + Vite. 런타임 CDN 없이 정적 빌드, 하위 경로에서도 동작하는 상대경로 자산.
- `frontend/src/graph/`: Canvas 렌더링, D3-force를 사용하는 Worker 배치, 순수 좌표/필터/인접 계산과 테스트.
- `frontend/src/`: 목록·검색·유형/분야 필터·상세·접근성·테마, 개발용 샘플.
- Python 수집기: 승인된 입력을 v2 데이터 계약으로 변환하고 빌드된 프론트엔드에 내장. 게시용 파일과 내부 보고/스냅샷을 분리.

## 소유권

- Sol graph: `frontend/src/graph/**`만 수정. 그래프 관련 단위 테스트 포함.
- Sol app: 나머지 `frontend/**`, 루트 `template.html`, 실행 안내 README, 프론트엔드 npm 의존성 설치/lock 소유.
- Terra data: `build.py`, `knowledge.py`, Python 테스트, `sample/**`, `config.example.json`, `requirements.txt`, `.gitignore`, 데이터 계약 문서.
- Astra: 본 계획과 공유 `frontend/src/model.ts`, 산출물/차이 검토, 브라우저 검수 및 통합 판단. 이슈 수정은 담당자에게 반환.

## 검증

1. TypeScript, 프로덕션 번들, 그래프 수학·인접 조회·레이아웃 테스트.
2. 기존 Python 테스트와 새 데이터 유형/프롬프트 제한/게시 파일 분리 테스트.
3. 승인된 샘플을 Python으로 실제 빌드한 결과를 브라우저로 열어 목록/검색/상세/전체·주변 그래프/줌/드래그 확인.
4. 개발 샘플 150개와 별도 1,000개 레이아웃 테스트에서 안정화·유한 좌표·반응성 확인. 측정 조건과 실제 수치를 기록하며 미측정 성능을 보장하지 않는다.
5. 기존 사용자 변경 보존, 원격 push·게시·알림 없음.

## 진행

- [x] 현행 저장소와 변경 여부 확인
- [x] 범위·데이터 계약·담당 파일 확정
- [x] 데이터 수집 연결
- [x] 그래프 엔진
- [x] 화면·빌드 환경
- [x] 통합·브라우저 검수

## 검수 기록

- Python 기존 회귀와 v2 데이터 테스트를 Astra가 재실행해 통과를 확인했다. 공개 산출물의 내부 보고서 제외, 출력 경로 경계, 명시적 프롬프트 자산, Git 실패 종료 코드가 검증 대상이다.
- 통합 검수에서 빈 메타데이터 로딩, StrictMode의 취소된 RAF 참조, 배치 완료 표시, 테마 전환 재그리기, 목록/필터 전환 위치 유지 문제를 발견해 수정 후 재확인했다.
- TypeScript 및 Vite 프로덕션 빌드 통과. 프런트엔드 테스트 15개 통과. 브라우저 검수는 Windows Chrome headless, 1440×900 및 390×844에서 실시했다.
- 150개 개발 샘플: 목록 전환과 검색 초기화 전후 Canvas 픽셀이 동일함을 확인했다. 줌·이동·상세·1단계 주변 그래프·테마·모바일 화면 및 전체 맞춤 버튼을 확인했다.
- 최종 Python 병합 샘플: 19개 자료, 14개 관계. 명시적 AI 프롬프트 표시, 업무 대시보드 원본 링크, 도구에 프롬프트가 없는 상태를 확인했다. 외부 링크 대상 서비스는 방문하지 않았다.
- 1,000개 노드/2,000개 관계: 최종 두 브라우저 측정은 약 3.1초와 3.7초에 Worker 완료 및 좌표 저장. 앞선 작업 병행 시 약 3.9~7.8초도 관찰했다. 이 수치는 이 PC의 개발 빌드 측정이며 FPS나 모든 기기의 성능 보장은 아니다. 계산 단위 테스트는 약 1.4~1.5초였다.
- 최종 기능 검수에서 JavaScript 예외 및 브라우저 콘솔 오류가 없었다. 기존 CSV의 승인 대기 2행, 중복 URL 1행, 분류 대기 1행은 검증 정책에 따라 제외됐다.
- 실제 Sheets/Notion 계정 수집, 링크 건강 검사, SSO/권한, 저장/업로드, 운영 배포는 미검증 또는 범위 밖이다.
